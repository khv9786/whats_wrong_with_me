spring:
 application:
  name : webIde
 output:
  ansi:
   enabled : always
 datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://localhost:3308/docker_root?
   username: root
   password : "!db1234"

 jpa:
  properties:
    hibernate:
      show_sql=true
      format_sql=true
  hibernate:
    ddl-auto: create
  database-platform : org.hibernate.dialect.MySQLDialect